# 后台 Agent 页面重设计方案

> 目标：系统 Agent 识别意图 → 指派领域 Agent；领域 Agent 可直接处理；绑定子 Agent 时也能识别并指派给对应子 Agent。
> 约束：不同类型的 Agent 不放在同一个页面。

---

## 一、第一性原理：根问题是什么

后台把 Agent 当成**扁平的资源列表**来管，但运行时它其实是一棵**调度树**。

这两者的错配，导致运营在后台永远回答不了三个真正要紧的问题：

| 关键问题 | 现在能不能在后台回答 |
| --- | --- |
| Q1 用户一句话进来，系统 Agent 会把它派给谁？ | 不能，要点进系统 Agent 的设置里翻路由规则 |
| Q2 这个领域 Agent 是自己干，还是往下派？派给哪个子 Agent？ | 不能，处理策略在设置页、子 Agent 关系完全不可见 |
| Q3 这条请求为什么被这样路由？ | 只能去"路由测试"单独试 |

所以**根问题不是"页面太挤"，而是 UI 的信息模型错了**：应该是「调度树编辑器」，不是「资源列表」。
按类型拆页面只是纠正这个模型的第一步，不是目的本身。

---

## 二、现状盘点（代码实证）

### 2.1 后端能力其实已经够了

| 层级 | 实现 | 关键代码 |
| --- | --- | --- |
| 系统 Agent → 通用/领域 Agent | 读 `route-copilot` 的 `routingRules`（管理员意图规则，**优先级最高**）+ LLM 在 `GENERAL/DOMAIN` 中挑选 | `AgentOrchestrator.routingPrompt()` L184-210 |
| 通用 Agent | 叶子节点，永远自己直接处理、不参与下级指派；同时是意图不明时的**兜底** | `decideDomain()` L94-96（非 `DOMAIN` 一律 DIRECT）；L209 无法判断时选 `assistant` |
| 领域 Agent → 自己处理 | `handlingMode = DIRECT` 直接返回自身 | `decideDomain()` L97-99 |
| 领域 Agent → 子 Agent（规则命中） | 每条绑定带 `routingRule` 关键词，`matchesRule()` 命中即指派，**优先级最高** | L110-114、L177-182 |
| 领域 Agent → 子 Agent（强制委派） | `handlingMode = DELEGATE` → 取候选列表第一个 | L115-118 |
| 领域 Agent → 子 Agent（自动决策） | LLM 在可用子 Agent 中选 | L119-140 |
| 兜底 | 领域 Agent 自己直接处理 | L144 |

**结论：四种角色（系统/通用/领域/子）的调度能力后端都已具备，本次主要是前端改造。**

还有一个区分会直接影响 UI 标注：`resolveUserAgent()` L78-84 与 `ApiController` L32 都只允许
`GENERAL`/`DOMAIN` 被插件直接选中 —— **系统 Agent(`MAIN`) 与子 Agent(`SUB`) 是内部节点**，用户不能直接选择。
所以页面上应把「通用 / 领域」标为**用户可选**，「系统 / 子」标为**内部节点**，这两类的定位截然不同：
通用 Agent 是与领域 Agent 平级的派发目标（且兼兜底），子 Agent 则是领域 Agent 的内部执行单元。

### 2.2 前端的三个问题

**问题 1：一页平铺，四种角色混在一起**

`main.tsx` L3139-3191 的 `tab === "agents"` 只按 `isSystemAgent` 布尔值分两组：

```
系统 Agent（systemAgent=true）  /  自定义（GENERAL + DOMAIN + SUB 全部混在这一组）
```

- 卡片上**没有任何角色标识**（L3163-3181 只有名称、id、描述、启停），不点进设置根本分不清是领域 Agent 还是子 Agent。
- 子 Agent 和它的父领域 Agent **平级展示**，父子关系在列表里完全看不见。

**问题 2：「领域 Agent 识别并指派给对应子 Agent」在 UI 上根本配不了**

后端 `AgentChildBinding` 有 `routingRule` 字段，`PUT /admin/agents/{id}/children` 收的是完整 `List<AgentChildBinding>`，**完全支持**每个子 Agent 配意图关键词。

但前端 L1887-1891 保存时只发了 `agentChildIds`（一个 `string[]`）：

```ts
agentChildIds.map((childAgentId, index) => ({
  childAgentId, priority: index * 10, enabled: true,
}))   // ← routingRule 从未被提交
```

结果：`routingRule` 永远是 null → 最可靠的「关键词命中」分支**永远走不到**。
`DELEGATE` 模式则是直接取 `candidates.get(0)`，即绑定顺序第一个，**与意图无关**。

**这就是"领域 Agent 识别不清楚、派不准子 Agent"的真正原因，也是必须优先修的功能缺口。**

**问题 3：上一次重命名把三层语义搞坏了**

上轮把 `SUB` 的显示名从「子 Agent」改成了「领域 Agent」，与 `DOMAIN` 重名。现在：

- `decideDomain()` 的 LLM 提示词变成
  `判断应该由领域 Agent 直接处理，还是委派给一个领域 Agent` —— 前后同义，模型无法理解；
- 候选列表提示变成 `只能选择以下领域 Agent`（原本是"子 Agent"）；
- 而本需求明确要求「领域 Agent → 子 Agent」是**两个不同层级**。

**必须把 `SUB` 的显示名改回「子 Agent」。** 这是本方案的前置条件。

---

## 三、目标信息架构

### 3.1 先定原则：导航可以共享，编辑必须按类型分离

"不同类型的 Agent 不要放在一个页面"约束的是**编辑表单**，不是**导航**。

四类 Agent 的配置项差异很大（系统配路由规则、领域配处理策略 + 子 Agent、通用/子只有基础信息），
硬塞进同一张表单只会互相干扰。但导航完全可以共享——**共享导航反而能把层级显式画出来**。

### 3.2 方案对比

| 方案 | 优点 | 问题 |
| --- | --- | --- |
| A. 侧边栏四个平级入口 | 实现最省事 | 侧边栏从 9 项涨到 12 项；四类被摆成"对等兄弟"，把调度树又压平了一次，与根问题自相矛盾 |
| B. 一个页面 + 滚动分段 | 点击最少 | 领域 + 子可达几十个，长滚动等于没有导航；子 Agent 脱离父领域上下文，正是要治的病 |
| C. 二级菜单（侧边栏折叠组） | 侧边栏只占 1 项，改动小 | 层级靠缩进表达，不如树直观 |
| **D. 树形导航 + 详情面板（推荐）** | 层级显式可见；不受量级影响；选中什么类型就出什么表单 | 需新增树组件，工作量最大 |

### 3.3 已定形态：一级 = 系统 Agent，二级 = 通用 / 领域 / 子

```
◆ Agent 配置  [系统]  ▾      ← 一级：点开即系统 Agent 控制台（单实例）
   ├ 通用 Agent               ← 二级：各自独立页面，只列该角色
   ├ 领域 Agent
   └ 子 Agent
▣ 知识库
⌘ MCP 服务
⚒ 工具 / ✦ Skill / ⚑ 钩子 / ★ 评分 / ☷ 对话日志 / ⌁ 路由测试
```

- 系统 Agent 是**单实例入口**，天然适合做「Agent 配置」的落地页：打开就是意图路由规则 +
  可派发的通用 / 领域清单 + 兜底说明，不必再套一层列表。
- 通用 / 领域 / 子收进二级，各占一个页面，互不混杂。
- 侧边栏入口数**保持 9 项不变**，折叠时与现状一致，不增加导航负担。
- 页面内容与表单逻辑与调度树方案完全一致，后续升级只改导航层。

### 3.4 未来可升级：方案 D（左侧调度树 + 右侧详情）

```
┌─ 调度树 ────────┬─ 详情（按选中类型出表单）─────────────┐
│ ① 系统 Agent    │  选中「领域」→ 基础信息 / 处理策略     │
│ ② 通用 Agent    │                / 子 Agent 调度 / 资源  │
│ ▾ ③ 报关领域    │                                        │
│     舱单核对    │  选中「子 Agent」→ 只有基础信息        │
│     税率查询    │  选中「通用」→ 基础信息（无调度配置）  │
│ ▸ ③ 物流领域    │                                        │
└─────────────────┴────────────────────────────────────────┘
```

- **层级来自真实数据**：子 Agent 的缩进直接用 `parentAgentId`（真实外键）；
  通用 / 领域挂在概念根节点下（后端本就不允许它们有父级，`AgentConfigurationService` L155-156）。
- **不同类型不共用表单**：选系统出路由规则，选领域出处理策略 + 子 Agent 调度，
  选通用 / 子则**不出现**对它们无效的配置。
- 量级再大也不怕：树可折叠，详情区永远只渲染一个 Agent。

### 3.5 层级关系（对应运行时调度树）

```
系统 Agent ──┬── 通用 Agent          （叶子：永远自己处理，意图不明时兜底）
             └── 领域 Agent ──┬── 自己直接处理
                              └── 子 Agent    （叶子：按意图关键词命中）
```

---

## 四、页面详细设计

### 页面 1：系统 Agent（一级落地页，单实例）

「Agent 配置」点开即本页。它不是列表，而是"入口控制台"。

- **身份区**：系统 Agent 卡片（不可删除、可启停），明确标注「请求入口」。
- **意图路由规则**（`routingRules`）：多行文本，含义是「意图 → 通用/领域 Agent」的映射说明，作为 LLM 的最高优先级依据。
- **可派发的 Agent**：列出当前已启用、可被指派的 `GENERAL`/`DOMAIN`，并**按类型分组**（通用 / 领域），点击可跳转。
  > 这块直接对应后端 `routingPrompt()` 的候选集，让"能派给谁"变成可见的。
- **兜底说明**：标注意图不明 / 置信度不足（`MIN_CONFIDENCE = 0.55`）时会落到 `assistant`，避免"派给了谁全靠猜"。
- **入口**：跳转「路由测试」。

### 页面 2：通用 Agent（列表）

通用 Agent 是**与领域 Agent 平级的派发目标**，但它是叶子节点，永远自己处理、不会再下派。

- 卡片显示：名称 / id / 描述 / 启停，并标注「用户可直接选中」。
- 详情页**不出现**「处理策略 / 子 Agent 调度」——`decideDomain()` 对非 `DOMAIN` 一律走 DIRECT，
  摆出来只会误导运营去配一个根本不生效的东西。
- 其余配置（基础信息 / 知识库 / 工具 / Skill / 版本）与领域 Agent 一致。
- 承担兜底职责的可打「兜底」徽标。

### 页面 3：领域 Agent（列表）

卡片上直接暴露调度信息（现在完全没有）：

- 名称 / id / 描述 / 启停
- **处理策略**徽标：直接处理 / 优先委派 / 自动决策
- **已绑定子 Agent 数**

点进详情，配置区按职责重排：

| 配置区 | 内容 |
| --- | --- |
| 基础信息 | 名称、描述、系统提示词、模型、温度、优先级 |
| **处理策略** | `handlingMode`：直接处理 / 优先委派 / 自动决策<br>`returnMode`：直接返回子 Agent 结果 / 由领域 Agent 总结 |
| **子 Agent 调度** | 见下 |
| 资源 | 知识库 / 工具 / Skill / 版本 |

**子 Agent 调度区（核心改造）**：绑定的每个子 Agent 一行，每行可配

- 子 Agent（下拉，仅 `SUB` 角色）
- **意图关键词 `routingRule`**（本次新增，逗号分隔）——即"什么样的话派给它"
- 优先级（决定 `DELEGATE` 时的顺序）
- 启用开关

并在面板上把**优先级顺序显式写出来**，消除"为什么派给了它"的困惑：

```
命中子 Agent 意图关键词  >  处理策略(优先委派取第一个)  >  LLM 自动决策  >  兜底：领域 Agent 自己处理
```

### 页面 4：子 Agent（列表）

- 卡片上显示**所属领域 Agent**（现在的平铺列表看不出来）。
- 顶部支持按「所属领域 Agent」筛选/分组。
- 新建子 Agent **必须选择父领域 Agent**（后端本就强制：`SUB` 必须有 parent，且只能绑一个）。
- 也可从页面 3 的「子 Agent 调度区」直接内联新建，列表页用于总览与批量维护。
- 标注「内部节点」：用户不能直接在插件里选中它。

---

## 五、需要改动的代码

### 前端 `admin/src/main.tsx`

| # | 改动 | 位置 |
| --- | --- | --- |
| 1 | `SUB` 显示名改回「子 Agent」（中英双语） | L219 / L553 及文案 |
| 2 | 「Agent 配置」一级落地页 = 系统 Agent；通用 / 领域 / 子作为二级菜单项 | L2315-2351 |
| 3 | `tab === "agents"` 单页 → 左树（按 `parentAgentId` 嵌套）+ 右详情（按角色出表单） | L3139-3191 |
| 4 | 卡片增加角色 / 处理策略 / 所属父级标识 | L3162-3182 |
| 5 | 子 Agent 绑定保存改为提交完整对象（含 `routingRule`） | L1883-1894 |
| 6 | 绑定 UI 增加「意图关键词」输入 | L2767 附近 |
| 7 | 新建子 Agent 强制选父领域 Agent | 新建流程 |
| 8 | 通用 Agent 详情隐藏「处理策略 / 子 Agent 调度」（对它不生效） | L2531-2558 config-tabs 条件 |

### 后端（改动很小）

| # | 改动 | 说明 |
| --- | --- | --- |
| 1 | `AgentOrchestrator` 提示词与文案改回「子 Agent」 | L108/113/122/124/137 等，恢复被重命名破坏的语义 |
| 2 | 可选：`DELEGATE` 且无规则命中时，改为按 `priority` 而非列表顺序 | 现在是 `candidates.get(0)`，未显式排序 |

> 后端**不需要**新增接口：`PUT /admin/agents/{id}/children` 已接收含 `routingRule` 的完整对象。

---

## 六、实施计划（按优先级）

### P0 — 不做则需求不成立

1. ✅ **`SUB` 显示名改回「子 Agent」**，并修复后端被破坏的提示词语义。已完成。
2. ✅ **子 Agent 绑定支持配置「意图关键词」并提交 `routingRule`**。已完成。
3. ✅ **菜单拆二级**：一级「Agent」= 系统 Agent 落地页；二级 = 通用 / 领域 / 子。已完成。

### P1 — 做完才好用

4. 领域 Agent 卡片显示处理策略与子 Agent 数量；子 Agent 卡片显示所属领域 Agent。
5. 领域 Agent 详情页「处理策略 + 子 Agent 调度」合并展示，并显式标注调度优先级顺序。
6. 新建子 Agent 强制选择父领域 Agent。
7. 卡片标注「用户可选 / 内部节点」；通用 Agent 详情隐藏不生效的调度配置，避免误导。
8. Agent 一级菜单点击一级按钮自身可切换展开/收起（默认展开），状态持久化到 localStorage；右侧 caret 单独保留作为显式折叠/展开入口；进入二级页面时若菜单是收起状态自动展开。

### P2 — 锦上添花

8. 系统 Agent 页展示"可派发的通用/领域 Agent"清单（按类型分组）并支持跳转，同时标注兜底去向。
9. 调度链可视化 / dry-run：输入一句话，直接看它会落到哪个子 Agent（复用现有路由测试能力）。

---

## 七、验收标准

- [ ] 选中不同类型时，详情区只出现对该类型生效的配置（通用/子不出现处理策略与子 Agent 调度）。
- [ ] 调度树中子 Agent 嵌套显示在所属领域 Agent 下。
- [ ] 通用 Agent 详情页不出现「处理策略 / 子 Agent 调度」（对它不生效，摆出来会误导）。
- [ ] 子 Agent 卡片能看到它属于哪个领域 Agent。
- [ ] 给子 Agent 配了意图关键词后，命中该关键词的请求确实被派到它（可用路由测试验证，链路显示 `命中子 Agent 指派规则`）。
- [ ] 领域 Agent 设为「直接处理」时，即使绑了子 Agent 也不会下派。
- [ ] 未命中任何规则、且未配置为优先委派时，由领域 Agent 自己处理。
- [ ] 中英文界面下「系统 / 通用 / 领域 / 子」四个名词含义一致、无重名。
- [ ] 通用 Agent 与领域 Agent 都能被系统 Agent 指派到；系统 Agent 与子 Agent 不能被插件直接选中。

---

## 八、已落地实现记录（本轮）

### 8.1 命名（P0-1）

- `SUB` → 「子 Agent / Sub-agent」；`MAIN` → 「系统 Agent / System Agent」；`GENERAL`「通用 Agent」；`DOMAIN`「领域 Agent」。
- 修复上一轮误改导致的语义塌陷：
  - `AgentOrchestrator.decideDomain()` 提示词恢复为「判断应该由领域 Agent 直接处理，还是委派给一个子 Agent」，候选列表前缀恢复为「只能选择以下子 Agent」。
  - `AgentConfigurationService` 校验文案恢复为「只有领域 Agent 可以绑定子 Agent」「子 Agent 的父级必须是领域 Agent」。
  - `ChatService` 恢复「子 Agent 返回结果（仅供参考）」。
  - `AgentRegistry` / `RouterAdminController` 保持「系统 Agent」。

### 8.2 子 Agent 意图关键词（P0-2）

- 前端新增 `agentChildRules: Record<string, string>` 状态，`openAgentSettings` 从 `GET /admin/agents/{id}/children` 回填 `routingRule`。
- 保存时提交 `routingRule`（此前只提交 `childAgentId`，导致 `matchesRule()` 这个最高优先级分支永远不命中）。
- 勾选子 Agent 后展开「意图关键词」输入框，支持中英文占位与说明。
- 后端无需改动：`AgentChildBinding.routingRule` 与 `PUT /{id}/children` 早已支持。

### 8.3 菜单拆二级（P0-3）

- 侧边栏「Agent」变为可折叠分组：一级进入系统 Agent 落地页，二级为「通用 Agent / 领域 Agent / 子 Agent」，折叠状态由 `agentMenuOpen` 控制，收起侧边栏时自动隐藏二级。
- 新增 tab：`agents`（系统，落地页）、`agents-general`、`agents-domain`、`agents-sub`；页头标题与分组说明按类型区分。
- `agentListTab` 按当前 tab 计算角色过滤：系统页取 `role === MAIN || isSystemAgent()`，其余按 `role` 精确匹配。
- 新建按钮按所在页面预置角色（`AGENT_PRESETS` 提供 MAIN / GENERAL / DOMAIN / SUB 的默认 ID、名称与系统提示词）。
- 从详情页返回时回到进入前的列表页（`agentReturnTab`）。
- 子 Agent 卡片显示所属领域 Agent 名称。
